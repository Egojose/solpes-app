export class Adjunto {


    id: number;
    filename: string;
    url: string;

    constructor(id: number, filename: string, url: string){
        this.id = id;
        this.filename = filename;
        this.url = url;
    }
}